<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= @article.title %> - Programming Blog</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            max-width: 900px;
            margin: 0 auto;
            padding: 20px;
            background-color: #f8f9fa;
            line-height: 1.7;
            color: #333;
        }
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px;
            border-radius: 10px;
            margin-bottom: 30px;
            text-align: center;
        }
        .header h1 {
            margin: 0 0 10px 0;
            font-size: 2.2em;
            line-height: 1.2;
        }
        .article-meta {
            opacity: 0.9;
            font-size: 14px;
        }
        .nav-link {
            display: inline-block;
            background-color: rgba(255,255,255,0.2);
            color: white;
            padding: 8px 16px;
            text-decoration: none;
            border-radius: 5px;
            margin-top: 15px;
            transition: background-color 0.3s;
        }
        .nav-link:hover {
            background-color: rgba(255,255,255,0.3);
        }
        .article-content {
            background: white;
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            margin-bottom: 30px;
        }
        .article-content h1,
        .article-content h2,
        .article-content h3,
        .article-content h4,
        .article-content h5,
        .article-content h6 {
            color: #333;
            margin-top: 30px;
            margin-bottom: 15px;
            line-height: 1.3;
        }
        .article-content h1 { font-size: 2em; border-bottom: 3px solid #667eea; padding-bottom: 10px; }
        .article-content h2 { font-size: 1.6em; border-bottom: 2px solid #667eea; padding-bottom: 8px; }
        .article-content h3 { font-size: 1.4em; color: #555; }
        .article-content p {
            margin-bottom: 20px;
            text-align: justify;
        }
        .article-content code {
            background-color: #f4f4f4;
            padding: 2px 6px;
            border-radius: 3px;
            font-family: 'Courier New', monospace;
            font-size: 0.9em;
        }
        .article-content pre {
            background-color: #f8f9fa;
            border: 1px solid #e9ecef;
            border-radius: 6px;
            padding: 20px;
            overflow-x: auto;
            margin: 20px 0;
        }
        .article-content pre code {
            background: none;
            padding: 0;
            border-radius: 0;
        }
        .article-content blockquote {
            border-left: 4px solid #667eea;
            padding-left: 20px;
            margin: 20px 0;
            color: #666;
            font-style: italic;
        }
        .article-content ul,
        .article-content ol {
            margin-bottom: 20px;
            padding-left: 30px;
        }
        .article-content li {
            margin-bottom: 8px;
        }
        .article-content table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
        }
        .article-content th,
        .article-content td {
            border: 1px solid #dee2e6;
            padding: 12px;
            text-align: left;
        }
        .article-content th {
            background-color: #f8f9fa;
            font-weight: 600;
        }
        .back-to-blog {
            text-align: center;
            margin-bottom: 30px;
        }
        .back-to-blog a {
            background: #6c757d;
            color: white;
            padding: 12px 24px;
            text-decoration: none;
            border-radius: 6px;
            transition: background-color 0.3s;
        }
        .back-to-blog a:hover {
            background: #5a6268;
        }
        @media (max-width: 768px) {
            .article-content {
                padding: 20px;
            }
            .header {
                padding: 20px;
            }
            .header h1 {
                font-size: 1.8em;
            }
        }
    </style>
</head>
<body>
    <div class="header">
        <h1><%= @article.title %></h1>
        <div class="article-meta">
            Published on <%= @article.created_at.strftime('%B %d, %Y') %>
        </div>
        <a href="/blog" class="nav-link">← Back to Blog</a>
    </div>

    <div class="back-to-blog">
        <a href="/blog">← View All Articles</a>
    </div>

    <div class="article-content">
        <% # Convert markdown to HTML (basic conversion) %>
        <%
        content = @article.content
        # Basic markdown to HTML conversion
        content = content.gsub(/^### (.*)$/, '<h3>\1</h3>')
        content = content.gsub(/^## (.*)$/, '<h2>\1</h2>')
        content = content.gsub(/^# (.*)$/, '<h1>\1</h1>')
        content = content.gsub(/\*\*(.*?)\*\*/, '<strong>\1</strong>')
        content = content.gsub(/\*(.*?)\*/, '<em>\1</em>')
        content = content.gsub(/`(.*?)`/, '<code>\1</code>')
        content = content.gsub(/^```(.*?)```$/m, '<pre><code>\1</code></pre>')
        content = content.gsub(/^> (.*)$/, '<blockquote>\1</blockquote>')
        content = content.gsub(/^(\d+)\. (.*)$/, '<li>\2</li>')
        content = content.gsub(/^- (.*)$/, '<li>\1</li>')
        content = content.gsub(/^([^<\n].*)\n\n/m, '<p>\1</p>' + "\n\n")
        %>
        <%= content %>
    </div>

    <div class="back-to-blog">
        <a href="/blog">← View All Articles</a>
    </div>
</body>
</html>
